# Shared Packages

Thư mục này dành cho package thật sự được nhiều application sử dụng, ví dụ shared API types hoặc lint configuration khi duplication đã xuất hiện.

Phase 01 chưa tạo package abstraction rỗng. Code domain của Web và API không được đưa vào đây chỉ để rút ngắn import path.
